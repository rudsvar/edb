/**
 * Variables are "boxes" to store values in.
 * We can refer to the values they have by using their names.
 */
void main() {
    var a = 12;
    var b = 48;
    var c = a + b;
    IO.println(c);
}
