/**
 * Ask the user for some input and modify it in some way.
 * As an example, print the string doubled. If the user writes hello, you can print hellohello.
 * Alternatively, read two lines of input (read twice) and print them in the opposite order.
 */
void main() {
}
