/**
 * IO also has a function called "readln" that prints a prompt and reads the answer.
 * We can store the answer in a variable.
 */
void main() {
    var name = IO.readln("Enter your name: ");
    IO.println("Hello " + name + "!");
}
